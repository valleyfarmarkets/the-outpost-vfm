import { Card } from "@/components/ui/card";

export default function LiveMusicDashboardPage() {
  return (
    <Card>
      <h2 className="text-2xl font-semibold text-gray-900">Live Music</h2>
      <p className="mt-2 text-gray-700">
        TODO: build CRUD for live_music_events table (create/edit, publish toggle, date/time inputs).
      </p>
    </Card>
  );
}
